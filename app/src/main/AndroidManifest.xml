<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meku"
        tools:targetApi="31">
        <activity
            android:name=".account_management_page.AccountDetailActivity"
            android:exported="false" />
        <activity
            android:name=".account_management_page.AccountSetManagementPageActivity"
            android:exported="false" />
        <activity
            android:name=".test_page.MainActivity2"
            android:exported="true"
            android:theme="@style/Theme.Meku" />
        <activity
            android:name=".initial_page.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".test_page.TestActivity"
            android:exported="true"
            android:theme="@style/Theme.Meku" />
    </application>

</manifest>